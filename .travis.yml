language: csharp

sudo: required
dist: trusty

env:
  - CLI_VERSION=1.0.0-preview1-002702

addons:
  apt:
    packages:
    - gettext
    - libcurl4-openssl-dev
    - libicu-dev
    - libssl-dev
    - libunwind8

mono:
  - latest

install:
  - export DOTNET_INSTALL_DIR="$PWD/.dotnetcli"
  - curl -sSL https://raw.githubusercontent.com/dotnet/cli/rel/1.0.0/scripts/obtain/dotnet-install.sh| bash /dev/stdin --version "$CLI_VERSION" --install-dir "$DOTNET_INSTALL_DIR"
  - export PATH="$DOTNET_INSTALL_DIR:$PATH"

script:
  - dotnet restore
  - dotnet restore linuxtest
  - dotnet build src/*
  - dotnet build test/*
  - for project in `find . -name '*.Tests'`; do mono ~/.nuget/packages/xunit.runner.console/2.1.0/tools/xunit.console.exe $project/Debug/bin/net45/$project.dll -parallel none; done
